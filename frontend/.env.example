# Vantage News Frontend Environment Variables

# Backend API Base URL (leave empty to use /api proxy, or set explicitly for remote backend)
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Minimum auto-refresh polling interval in milliseconds (prevents hammering backend)
# NEXT_PUBLIC_MIN_POLL_INTERVAL_MS=15000
